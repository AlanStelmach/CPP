#include <iostream>
#include <list>
#include <vector>
#include <set>
#include <map>
#include <algorithm>

using namespace std;

int main()
{
    vector <int> v;
    v.push_back(5);
    v.push_back(7);
    v.push_back(1);
    v.push_back(3);
    sort(v.begin(),v.begin()+3);
    for(int i=0; i<v.size(); i++)
        cout<<v[i]<<endl;
    return 0;
}
